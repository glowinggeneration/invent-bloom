# Exception Register — SMAIT

Per Application Build Master Rules §16. Every rule not currently met is
recorded here with an owner and a deadline. Exceptions expire — they are
not permanent permission to skip the standard. This register was seeded
during the initial Master Rules adoption pass; update it (don't just
accumulate) as items close or new ones are found.

| Rule                                                                            | Reason                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Risk                                                                                                                                                                                                                                                                                                                                                | Temporary control                                                                                                                                                                                                                | Owner | Deadline | Approval |
| ------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----- | -------- | -------- |
| §4.4 / §12.3 Console error on every signed-out cold load                        | Confirmed pre-existing (unrelated to any file touched during the SMAIT rebuild). Reproduced live on `/overview`, `/mentions`, `/watchlist` with no session. Investigated further this pass with the raw SSR HTML for `/overview`: the server correctly emits empty `<Suspense>` markers for the whole `_authenticated` subtree (as `ssr: false` intends). Tried gating `_authenticated/route.tsx`'s `<Outlet />` behind a post-mount flag so the client's first paint also renders nothing, matching the server - this did not fix it and was reverted, since it added an unproven flash-of-blank on every authenticated navigation without resolving the error. That result narrows the cause to a router-level race, not a component-render mismatch: `beforeLoad`'s `supabase.auth.getUser()` check resolves and its redirect to `/auth` fires during/before hydration, so the client ends up hydrating `/auth`'s real sign-in form against the server's empty `/overview` placeholder - a matched-route mismatch, not a content mismatch. Fixing that needs either a TanStack Router API for deferring `beforeLoad` redirects until after hydration, or an upstream fix (project is on `@tanstack/react-router` 1.170.18 / `react-start` 1.168.32, both pre-1.0-adjacent betas). | Purely a logged console error in current testing - React recovers by discarding the server HTML and re-rendering client-side, and every redirect (to `/auth`, `/setup`) still lands correctly. Violates the "no console errors on core journeys" release gate as currently written, and could mask a real regression under the same log line later. | None active; relies on React's built-in hydration-mismatch recovery.                                                                                                                                                             | TBD   | TBD      | TBD      |
| §6.4 MFA for the admin account — partial                                        | Admin sign-in now supports opt-in TOTP via Supabase Auth's native MFA API (enroll/challenge/verify/unenroll), managed from a new card on Admin Profile (`src/components/admin-mfa.tsx`) and enforced at login (`src/routes/auth.tsx`): after password sign-in, if the session's next assurance level is aal2 and it hasn't reached it yet, the user must enter a 6-digit code before the app navigates anywhere. No secret or code ever touches this app's own server code - Supabase Auth owns enrollment, storage and verification. Two things remain: enforcement is client-side only (no server-side `assertAdmin`/RLS check confirms a request's session actually reached aal2, since getting that check wrong across the ~90 admin call sites without live testing risked breaking all of them for an unverifiable benefit), and there's no in-app recovery-code flow - losing the authenticator means removing the factor from the Supabase dashboard directly, which is disclosed in the UI. Could not be tested end-to-end live (no admin test credentials); verified via typecheck against Supabase's real MFA types, a clean console on the unauthenticated path, and full validate.                                                                                      | A malicious actor with valid credentials but no server-side origin check could, in principle, drive a state-changing admin action through something other than this app's own login form without ever completing the MFA step.                                                                                                                      | The password itself is still required, and the login-form path (the only one this app exposes) does enforce the step-up.                                                                                                         | TBD   | TBD      | TBD      |
| §9.1 Multi-provider AI fallback                                                 | Deliberately not wired through `ai-provider.server.ts`. Only one AI provider is configured anywhere in this project (the Lovable gateway via `LOVABLE_API_KEY`) - there is no second provider to fail over to, so wrapping calls in the fallback adapter now would be cosmetic, not functional. The scaffold's `AiRequest` shape (string-only message content) also doesn't fit this app's real multimodal (image) calls without extending it. Revisit once a second provider is actually available.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | If the Lovable gateway is unavailable, AI-dependent features fail outright rather than degrading.                                                                                                                                                                                                                                                   | Calls fail closed with a specific user-facing error (rate-limited / credits-exhausted / malformed) rather than a silent failure.                                                                                                 | TBD   | TBD      | TBD      |
| §9.2 Structured/validated AI outputs                                            | `draftReplies()` parses model output with a regex (`content.match(/\{[\s\S]*\}/)`) rather than a schema-constrained response.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | A malformed or adversarial model response could silently produce no drafts, or (in a future feature) unexpected fields.                                                                                                                                                                                                                             | Parse failures are caught and fall back to an empty result.                                                                                                                                                                      | TBD   | TBD      | TBD      |
| §9.7 Token caps and cost controls                                               | No per-request or per-user/workspace AI token cap is enforced. Tracking this together with the multi-tenant conversion's plan-tier/usage-limit work (Phase 4) rather than as a separate effort - both land the same underlying usage-counting/enforcement mechanism, just scoped per-workspace once workspaces exist.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | An unbounded prompt or a runaway retry loop could generate uncontrolled AI spend.                                                                                                                                                                                                                                                                   | Requests are single-shot, not retried automatically. Rate limiting (see §6.5 row) now bounds request _frequency_ for the persona-panel test, though not per-request token size.                                                  | TBD   | TBD      | TBD      |
| Multi-tenant conversion — storage bucket policies unverifiable from this repo   | Storage bucket definitions (public/private flags, any `storage.objects` policies set via the Supabase dashboard rather than migrations) live outside version control. One bucket (`message-uploads`) has zero policy in the migration history at all - it's either dashboard-configured or has no client grant, and this can't be told apart from a static read of the repo. No service-role key or dashboard access is available in this environment to check directly.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Phase 2's storage-tenancy remediation (rewriting the `managed-reports` bucket's cross-tenant-readable policy) can't be fully verified complete without also confirming no other undocumented bucket policy exists with the same shape.                                                                                                              | The one concrete leak found in research (`managed-reports`) is remediated in Phase 2 regardless; this row tracks the residual "are there others we can't see" risk until a live dashboard check happens.                         | TBD   | TBD      | TBD      |
| §9.8 AI observability — partial                                                 | `ai-observability.server.ts` is now wired into `smait.server.ts`'s `callGateway()` (persona panel + synthesis - the single most central and highest-cost AI feature), recording provider/model/tokens/latency/outcome to `ai_events` on every call, success or failure. The other 7 direct-fetch AI call sites (`variations.server.ts`, `translation.server.ts`, `campaigns.server.ts`, `always-on.server.ts`, `reply-suggestion.server.ts`, `campaign-naming.server.ts`, `legal-risk.server.ts`) are not yet instrumented.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Cost/failure patterns for those 7 call sites aren't queryable from `ai_events`; incidents there are diagnosed from application logs only.                                                                                                                                                                                                           | Errors surface in application logs (unstructured) for the unwired call sites.                                                                                                                                                    | TBD   | TBD      | TBD      |
| §3.1 / §8.2 — 306 untyped (`any`) values in API/scheduler/provider glue         | Twitter API glue, scheduler and reports code use `any` at ~300 call sites (see prior audit). Rewriting all of them without a live API/DB to verify against risked introducing bugs for cosmetic gain.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Loose typing hides shape mismatches at compile time; bugs surface at runtime instead.                                                                                                                                                                                                                                                               | Runtime `tsc`/build/tests all pass; behaviour unchanged since these are pre-existing.                                                                                                                                            | TBD   | TBD      | TBD      |
| §12.1 Full-project lint gate is not green                                       | Baseline lint debt predates this engagement (formatting, explicit `any`, hook-order violations across legacy files). Every file touched during the SMAIT rebrand/generalization/feature work was formatted and linted directly, which incidentally reduced the total count from ~2,211 to ~470 problems, but the gate is still not green project-wide.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | New regressions can be obscured by the volume of existing findings.                                                                                                                                                                                                                                                                                 | Each changed file is formatted and linted directly. The test suite, production build, TypeScript check and changed-file lint pass. Reduce the baseline in isolated changes before enabling lint as a release gate.               | TBD   | TBD      | TBD      |
| §5.3 Hardcoded production fallback in `vite.config.ts`                          | `publicBackendUrl`/`publicBackendKey` fall back to a literal prod Supabase URL and publishable key if `VITE_SUPABASE_URL`/`VITE_SUPABASE_PUBLISHABLE_KEY` are absent, with an existing comment explaining this covers a real gap where "older deploy workers may omit the managed VITE_* aliases." Deliberately not removed this pass: the value is a public, non-secret browser connection identifier (not a credential), and blindly stripping the fallback risks a production outage on any deploy target that still hits the documented gap, which cannot be verified without access to Lovable's live deploy infrastructure.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | If ever swapped to a different Supabase project, a deploy target missing the managed alias would silently keep talking to the old project instead of failing loudly.                                                                                                                                                                                | Value is public/non-secret; a stale fallback fails soft (wrong-but-working project) rather than hard.                                                                                                                            | TBD   | TBD      | TBD      |
| §14 Performance — no true before/after baseline, authenticated pages unmeasured | No performance measurement was captured before Stage 4/5 changes began, so Stage 6 could only report current-state numbers, not deltas. Live performance testing on authenticated pages (Overview, Mentions) was blocked for the same reason as Stage 4's UI audit — no service-role key or test credentials were available to sign in and drive those routes in a browser.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Regressions introduced by earlier stages on authenticated pages specifically could not be measured or ruled out this pass.                                                                                                                                                                                                                          | Production build succeeds; `pdfjs-dist`/`mammoth` confirmed code-split into on-demand chunks (not in the main bundle) via build output inspection; public-page (`/`, `/auth`) load was measured directly with no console errors. | TBD   | TBD      | TBD      |

## How to use this register

1. When a Master Rule can't be met before a release, add a row here instead of silently shipping the gap.
2. Fill in **Owner** and **Deadline** before merging — "TBD" is a placeholder for the adoption pass, not a resting state.
3. When an item is resolved, delete the row (git history keeps the record) rather than marking it "done" in place.
4. This file should shrink over time. A growing register is a signal to stop adding scope and pay down risk instead.
