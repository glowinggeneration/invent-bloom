# Exception Register — SMAIT

Per Application Build Master Rules §16. Every rule not currently met is
recorded here with an owner and a deadline. Exceptions expire — they are
not permanent permission to skip the standard. This register was seeded
during the initial Master Rules adoption pass; update it (don't just
accumulate) as items close or new ones are found.

| Rule                                                                                                                    | Reason                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Risk                                                                                                                                                                                                                                                                                                                                                                                                                 | Temporary control                                                                                                                                                                                                                                                                           | Owner | Deadline | Approval |
| ----------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----- | -------- | -------- |
| §4.4 / §12.3 Console error on every signed-out cold load                                                                | Confirmed pre-existing (unrelated to any file touched during the SMAIT rebuild). Reproduced live on `/overview`, `/mentions`, `/watchlist` with no session. Investigated further this pass with the raw SSR HTML for `/overview`: the server correctly emits empty `<Suspense>` markers for the whole `_authenticated` subtree (as `ssr: false` intends). Tried gating `_authenticated/route.tsx`'s `<Outlet />` behind a post-mount flag so the client's first paint also renders nothing, matching the server - this did not fix it and was reverted, since it added an unproven flash-of-blank on every authenticated navigation without resolving the error. That result narrows the cause to a router-level race, not a component-render mismatch: `beforeLoad`'s `supabase.auth.getUser()` check resolves and its redirect to `/auth` fires during/before hydration, so the client ends up hydrating `/auth`'s real sign-in form against the server's empty `/overview` placeholder - a matched-route mismatch, not a content mismatch. Fixing that needs either a TanStack Router API for deferring `beforeLoad` redirects until after hydration, or an upstream fix (project is on `@tanstack/react-router` 1.170.18 / `react-start` 1.168.32, both pre-1.0-adjacent betas). | Purely a logged console error in current testing - React recovers by discarding the server HTML and re-rendering client-side, and every redirect (to `/auth`, `/setup`) still lands correctly. Violates the "no console errors on core journeys" release gate as currently written, and could mask a real regression under the same log line later.                                                                  | None active; relies on React's built-in hydration-mismatch recovery.                                                                                                                                                                                                                        | TBD   | TBD      | TBD      |
| §6.4 MFA for the admin account — partial                                                                                | Admin sign-in now supports opt-in TOTP via Supabase Auth's native MFA API (enroll/challenge/verify/unenroll), managed from a new card on Admin Profile (`src/components/admin-mfa.tsx`) and enforced at login (`src/routes/auth.tsx`): after password sign-in, if the session's next assurance level is aal2 and it hasn't reached it yet, the user must enter a 6-digit code before the app navigates anywhere. No secret or code ever touches this app's own server code - Supabase Auth owns enrollment, storage and verification. Two things remain: enforcement is client-side only (no server-side `assertAdmin`/RLS check confirms a request's session actually reached aal2, since getting that check wrong across the ~90 admin call sites without live testing risked breaking all of them for an unverifiable benefit), and there's no in-app recovery-code flow - losing the authenticator means removing the factor from the Supabase dashboard directly, which is disclosed in the UI. Could not be tested end-to-end live (no admin test credentials); verified via typecheck against Supabase's real MFA types, a clean console on the unauthenticated path, and full validate.                                                                                      | A malicious actor with valid credentials but no server-side origin check could, in principle, drive a state-changing admin action through something other than this app's own login form without ever completing the MFA step.                                                                                                                                                                                       | The password itself is still required, and the login-form path (the only one this app exposes) does enforce the step-up.                                                                                                                                                                    | TBD   | TBD      | TBD      |
| §9.1 Multi-provider AI fallback                                                                                         | Deliberately not wired through `ai-provider.server.ts`. Only one AI provider is configured anywhere in this project (the Lovable gateway via `LOVABLE_API_KEY`) - there is no second provider to fail over to, so wrapping calls in the fallback adapter now would be cosmetic, not functional. The scaffold's `AiRequest` shape (string-only message content) also doesn't fit this app's real multimodal (image) calls without extending it. Revisit once a second provider is actually available.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | If the Lovable gateway is unavailable, AI-dependent features fail outright rather than degrading.                                                                                                                                                                                                                                                                                                                    | Calls fail closed with a specific user-facing error (rate-limited / credits-exhausted / malformed) rather than a silent failure.                                                                                                                                                            | TBD   | TBD      | TBD      |
| §9.2 Structured/validated AI outputs                                                                                    | `draftReplies()` parses model output with a regex (`content.match(/\{[\s\S]*\}/)`) rather than a schema-constrained response.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | A malformed or adversarial model response could silently produce no drafts, or (in a future feature) unexpected fields.                                                                                                                                                                                                                                                                                              | Parse failures are caught and fall back to an empty result.                                                                                                                                                                                                                                 | TBD   | TBD      | TBD      |
| §9.7 Token caps and cost controls                                                                                       | No per-request AI token cap is enforced. Phase 4 landed the shape this was tracked against — `plan_limits` (accounts/seats/keywords/AI-calls-per-month by tier) plus a live usage query surfaced on the workspace's Plan & usage panel — but by explicit scope decision it's display-only: no action is ever refused for being over a limit, since there's no real billing yet and a hard block would trap a workspace with no way to actually upgrade. See the "plan-tier enforcement is soft-only" row below for that decision's own risk.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | An unbounded prompt, a runaway retry loop, or simply heavy real usage past a workspace's nominal plan limit could generate uncontrolled AI spend with nothing to stop it.                                                                                                                                                                                                                                            | Requests are single-shot, not retried automatically. Rate limiting (see §6.5 row) bounds request _frequency_ for the persona-panel test, though not per-request token size or monthly spend.                                                                                                | TBD   | TBD      | TBD      |
| Multi-tenant conversion — RLS rewrite unverified against a live database                                                | Phase 2b rewrote every table's RLS policy from the old `org='team'`/`USING(true)` patterns to `workspace_id = private.current_workspace_id()`, and Phase 2c found and fixed several real constraint bugs (composite-key collisions, a per-request handler that silently wrote to the wrong workspace). All of it was validated by reading the SQL and reasoning about it — there is no live Supabase/Postgres access in this environment, so no policy has ever actually been exercised against a running database. `bunx tsc`/tests/build passing proves the TypeScript compiles and matches generated types; it proves nothing about whether Postgres actually enforces these policies as written.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | If any policy has a logic error not visible from a static read (a wrong operator, a missing `WITH CHECK`, a policy that doesn't fire for the client role actually used), it would only surface once a second real workspace exists and either can't see its own data or can see another's.                                                                                                                           | Migrations were written against the exact current-state policy definitions (read from the live migration history, not guessed) and reviewed for the specific bug classes already found once (natural-key collisions, unscoped writes). No live enforcement test exists yet.                 | TBD   | TBD      | TBD      |
| Multi-tenant conversion — migration history duplication never resolved                                                  | Phase 0 flagged an apparent squash event in the migration folder — two overlapping generations of `CREATE TABLE` for the same tables (e.g. `apify_mentions`, `reports`, `news_articles` each defined twice, in an early migration and again in a later "squashed" one). All Phase 2 work assumed the later generation is canonical, since it's the one Phase 2b's exact policy names were read against — but this was never confirmed with `supabase db diff` or an equivalent live replay, because no live Supabase project is reachable from this environment.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | If the assumption is wrong, or if the live project's actual applied-migration history diverges from what's in this repo, some or all of Phase 1/2/4's migrations could fail to apply cleanly, or could apply against different starting assumptions than the code expects.                                                                                                                                           | None active — purely an assumption based on internal consistency of the migration files, not a live check.                                                                                                                                                                                  | TBD   | TBD      | TBD      |
| Multi-tenant conversion — plan-tier enforcement is soft-only, billing is fake                                           | Explicit scope decision at the start of this conversion ("build the shape, fake the plans"): `workspaces.plan_tier` and `plan_limits` exist, usage is computed and shown against them, and only the platform admin can change a workspace's tier (a plain `UPDATE`, no payment processor). No action anywhere is ever blocked for being over a plan's limit.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | A workspace can exceed its nominal plan's caps indefinitely with no consequence beyond a UI number turning amber — this is fine for now but means "plan tier" currently has no functional effect on the product, only on what's displayed.                                                                                                                                                                           | Usage is visible in the UI (Profile → Plan & usage) so the gap is honest, not hidden.                                                                                                                                                                                                       | TBD   | TBD      | TBD      |
| Multi-tenant conversion — team invite requires an existing account                                                      | `inviteWorkspaceMember` (Phase 4) can only add someone who already has an account somewhere on the platform — it looks them up by email in `profiles` and fails with a specific message if no match exists. There is no email-invite infrastructure (no transactional email sending is wired into this app at all), so a true "invite anyone by email" flow was out of scope for this pass.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | A workspace owner cannot bring on a genuinely new person without first asking them to sign up (which creates that person their own separate workspace) and then inviting them into the existing one — an awkward two-step, not a dead end, but not the flow a typical SaaS invite implies.                                                                                                                           | The failure message tells the inviter exactly what to do ("ask them to create an account first, then invite them").                                                                                                                                                                         | TBD   | TBD      | TBD      |
| Multi-tenant conversion — no live QA of the new multi-tenant flows                                                      | Self-serve signup, the per-workspace team/plan dashboard, and the cross-tenant SaaS-owner dashboard (Phases 3-4) were verified via typecheck/tests/build and static review only. No one has signed up a second real workspace, invited a member, or changed a plan tier through the actual running app in a browser — this environment has no login credentials and no live database to create real test accounts against.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Any bug specific to actual runtime behavior (a race condition, a real Postgres error message not matching the code's assumptions, an RLS policy that behaves differently than its migration reads) would not have been caught.                                                                                                                                                                                       | Every server function follows the same patterns already proven correct elsewhere in the codebase (`resolveWorkspaceId`, `assertAdmin`, the same Supabase client conventions), and the isolation-critical chokepoint (`resolveWorkspaceId`) has unit test coverage.                          | TBD   | TBD      | TBD      |
| Multi-tenant conversion — Apify/news sweeps ship unconfigured, not workspace-aware                                      | `apify-mentions.server.ts` and `news.server.ts`'s search query lists (`SEARCH_QUERIES`, `NEWS_QUERIES`, `WATCHED_PROFILES`, etc., in `apify-relevance.server.ts`/`news.ts`) are hardcoded empty arrays by a pre-existing design decision that predates the multi-tenant conversion — the workspace ships generic/unconfigured until these are wired to the Monitoring Watchlist. Both sweeps still write under `LEGACY_SINGLE_WORKSPACE_ID` rather than real per-workspace resolution, left that way deliberately: looping them per workspace today would run an empty query once per workspace, for no behavior change.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Once these lists are eventually wired to the Monitoring Watchlist (tracked here as the trigger for that work), the `LEGACY_SINGLE_WORKSPACE_ID` writes must be replaced with real per-workspace iteration in the same change, or every workspace's Apify/news results would silently land under Workspace 1 instead of their own — the same bug class already found and fixed for `activeKeywords`/`storeXMentions`. | The query lists being empty means the sweeps are currently no-ops for every workspace including the original one, so there is no live leak today.                                                                                                                                           | TBD   | TBD      | TBD      |
| Multi-tenant conversion — storage bucket policies unverifiable from this repo                                           | Storage bucket definitions (public/private flags, any `storage.objects` policies set via the Supabase dashboard rather than migrations) live outside version control. One bucket (`message-uploads`) has zero policy in the migration history at all - it's either dashboard-configured or has no client grant, and this can't be told apart from a static read of the repo. No service-role key or dashboard access is available in this environment to check directly.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Phase 2's storage-tenancy remediation (rewriting the `managed-reports` bucket's cross-tenant-readable policy) can't be fully verified complete without also confirming no other undocumented bucket policy exists with the same shape.                                                                                                                                                                               | The one concrete leak found in research (`managed-reports`) is remediated in Phase 2 regardless; this row tracks the residual "are there others we can't see" risk until a live dashboard check happens.                                                                                    | TBD   | TBD      | TBD      |
| §9.8 AI observability — partial                                                                                         | `ai-observability.server.ts` is now wired into `smait.server.ts`'s `callGateway()` (persona panel + synthesis - the single most central and highest-cost AI feature), recording provider/model/tokens/latency/outcome to `ai_events` on every call, success or failure. The other 7 direct-fetch AI call sites (`variations.server.ts`, `translation.server.ts`, `campaigns.server.ts`, `always-on.server.ts`, `reply-suggestion.server.ts`, `campaign-naming.server.ts`, `legal-risk.server.ts`) are not yet instrumented.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Cost/failure patterns for those 7 call sites aren't queryable from `ai_events`; incidents there are diagnosed from application logs only.                                                                                                                                                                                                                                                                            | Errors surface in application logs (unstructured) for the unwired call sites.                                                                                                                                                                                                               | TBD   | TBD      | TBD      |
| §3.1 / §8.2 — 306 untyped (`any`) values in API/scheduler/provider glue                                                 | Twitter API glue, scheduler and reports code use `any` at ~300 call sites (see prior audit). Rewriting all of them without a live API/DB to verify against risked introducing bugs for cosmetic gain.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Loose typing hides shape mismatches at compile time; bugs surface at runtime instead.                                                                                                                                                                                                                                                                                                                                | Runtime `tsc`/build/tests all pass; behaviour unchanged since these are pre-existing.                                                                                                                                                                                                       | TBD   | TBD      | TBD      |
| §12.1 Full-project lint gate is not green                                                                               | Baseline lint debt predates this engagement (formatting, explicit `any`, hook-order violations across legacy files). Every file touched during the SMAIT rebrand/generalization/feature work was formatted and linted directly, which incidentally reduced the total count from ~2,211 to ~470 problems, but the gate is still not green project-wide.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | New regressions can be obscured by the volume of existing findings.                                                                                                                                                                                                                                                                                                                                                  | Each changed file is formatted and linted directly. The test suite, production build, TypeScript check and changed-file lint pass. Reduce the baseline in isolated changes before enabling lint as a release gate.                                                                          | TBD   | TBD      | TBD      |
| §5.3 Hardcoded production fallback in `vite.config.ts`                                                                  | `publicBackendUrl`/`publicBackendKey` fall back to a literal prod Supabase URL and publishable key if `VITE_SUPABASE_URL`/`VITE_SUPABASE_PUBLISHABLE_KEY` are absent, with an existing comment explaining this covers a real gap where "older deploy workers may omit the managed VITE_* aliases." Deliberately not removed this pass: the value is a public, non-secret browser connection identifier (not a credential), and blindly stripping the fallback risks a production outage on any deploy target that still hits the documented gap, which cannot be verified without access to Lovable's live deploy infrastructure.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | If ever swapped to a different Supabase project, a deploy target missing the managed alias would silently keep talking to the old project instead of failing loudly.                                                                                                                                                                                                                                                 | Value is public/non-secret; a stale fallback fails soft (wrong-but-working project) rather than hard.                                                                                                                                                                                       | TBD   | TBD      | TBD      |
| §14 Performance — no true before/after baseline, authenticated pages unmeasured                                         | No performance measurement was captured before Stage 4/5 changes began, so Stage 6 could only report current-state numbers, not deltas. Live performance testing on authenticated pages (Overview, Mentions) was blocked for the same reason as Stage 4's UI audit — no service-role key or test credentials were available to sign in and drive those routes in a browser.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Regressions introduced by earlier stages on authenticated pages specifically could not be measured or ruled out this pass.                                                                                                                                                                                                                                                                                           | Production build succeeds; `pdfjs-dist`/`mammoth` confirmed code-split into on-demand chunks (not in the main bundle) via build output inspection; public-page (`/`, `/auth`) load was measured directly with no console errors.                                                            | TBD   | TBD      | TBD      |
| §13 Deployment — visual-language pass shipped directly to `main`, not on feature branches with small reviewable commits | This environment auto-syncs bidirectionally with the connected Lovable editor: every commit made to `main` during the session (dark-mode default, the sign-in redesign, the ~40-route glow restyle, nine MagicUI-style components, notification/profile/setup restyles) was pushed to `origin/main` in real time as it landed, confirmed by `git log origin/main` matching local HEAD exactly. By the time this gap was noticed, the commits were already published, so per AGENTS.md's explicit rule (never rewrite published history — it corrupts the Lovable-side project history), they could not be retroactively split into a feature branch. Each stage was still committed as its own reviewable unit with a descriptive message and its own typecheck/lint/test/build pass, and one real incident (a `git stash` run by a parallel background agent that briefly discarded other agents' uncommitted work) was caught and fully recovered before landing — but the branch-per-stage and small-commit-size parts of the standard were not followed.                                                                                                                                                                                                                         | A change with a real defect ships straight to the synced main branch with no PR review gate in between; reverting one stage in isolation is harder when several route files landed in one commit rather than one per reviewable change.                                                                                                                                                                              | Every commit still passed its own typecheck/lint/test/build gate before landing, and the working tree was independently re-verified as one clean batch after each parallel-agent wave, catching the stash incident before it reached `main`. Feature-branch workflow adopted going forward. | TBD   | TBD      | TBD      |
| §5.2 / §12.2 Five-destination IA rework — no live keyboard/screen-reader/responsive verification                        | Static review only for the new Today page and restructured sidebar/mobile-nav/command-palette: real `<button>`/`<Link>` elements throughout (native keyboard operability), `aria-label`/`aria-expanded`/`aria-controls`/`role="group"` added and reasoned about by hand, `tabIndex` managed on the composer's not-yet-actionable workflow buttons. No actual screen reader, no actual keyboard-only walkthrough, no actual phone-width device test was performed — this environment has no session credentials to sign in past the auth guard in a real browser.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | An interaction that looks correct in source (focus order, announced state, touch-target spacing at 5 equal mobile tabs) could still behave wrong in a real assistive-technology pass; this wouldn't be caught until a human verifies it.                                                                                                                                                                             | Automated `tsc`/eslint/vitest/build all pass; the accessible-name gap on compact-rail icon buttons was found and fixed by reasoning about the actual Tooltip implementation's DOM output, not assumed correct.                                                                              | TBD   | TBD      | TBD      |
| Projects feature — cross-workspace isolation and switching verified by construction, not live multi-workspace testing   | `projects`, `project_references` and `investigations` all use the standard `private.can_access_workspace(workspace_id)` RLS policy, `linkItemToProject` explicitly re-checks the target project's `workspace_id` against `resolveWorkspaceId(context)` before linking (a gap found and closed during this stage — the FK alone only guarantees the project exists, not that it's the caller's), and the active-project selector is a client-only localStorage id that is provably inert on its own: every read still goes through the same RLS-scoped server functions regardless of what a tampered id contains. None of this was exercised against a live second workspace — no test credentials in this environment, the same constraint noted in every prior multi-tenant row above.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | If a policy or the `linkItemToProject` check has a logic error not visible from a static read, it would only surface once a second real workspace links or views project data live.                                                                                                                                                                                                                                  | Same pattern already proven correct elsewhere in the codebase (`resolveWorkspaceId` + RLS on every write); the one gap found by reasoning through the flow (unchecked cross-workspace link target) was fixed in this stage, not left open.                                                  | TBD   | TBD      | TBD      |

| Knowledge Library — conflict detection is model-reasoned, not deterministic | Studio's synthesis prompt is given approved Knowledge Library entries and asked to flag contradictions via a `knowledgeConflicts` field; results are filtered so only an id actually supplied as context can be cited (hallucination guard), but the contradiction judgment itself is an LLM inference, not a rules engine — it can miss a real conflict or flag a non-conflict. | A reviewer could ship a draft that actually contradicts approved guidance if the model doesn't surface it, or waste time on a false positive. | Absence of a flag is documented in the UI copy as "nothing was checked", never "no conflict exists" (see the `knowledgeConflicts` JSDoc in `src/lib/analysis.ts`); resolving a real conflict still requires a human to edit either the draft or the outdated knowledge entry. | TBD | TBD | TBD |
| Growth instrumentation — foundational only, most of the requested funnel is unmeasured | Per the growth-engineering mandate added mid-session, a minimal `product_events` table and `trackEvent()` helper were built (no prior analytics infrastructure existed anywhere in this codebase) and wired into 3 real events: `project_created`, `investigation_saved` (proxy for "first useful result"), `knowledge_entry_approved`. The full requested event set — `signup_completed`, `report_shared`, `collaborator_joined`, `workspace_returned`, `paid_conversion` — is defined in the `GrowthEventName` type but not yet wired to any code path; no dashboard or funnel view reads `product_events` yet; no baseline, experiment, or "weekly workspaces completing a meaningful task" metric has been established. | Growth claims cannot yet be evidenced — there is no data to validate the "investigation_saved as first-useful-result proxy" hypothesis, and several of the funnel's most important moments (signup, sharing, return visits, conversion) aren't instrumented at all. | Every write goes through `supabaseAdmin` only (never the client), and string properties are truncated to 60 chars - confidential content cannot reach this table even by mistake. | TBD | TBD | TBD |

| Today's priorities — no live keyboard/screen-reader/mobile verification | Priority rows use native `<button>`/`<Link>` elements with `aria-label` on every icon-only control (move up/down, complete, defer, remove), and the defer/limit popovers are Radix `Popover` (keyboard-operable by construction). No actual screen reader, keyboard-only walkthrough or phone-width device test was performed — same constraint as every prior UI stage in this register: no session credentials to sign in past the auth guard in a real browser. | An interaction correct in source (focus order, announced state) could still behave wrong in a real assistive-technology pass. | Automated `tsc`/eslint/vitest/build all pass; the same aria-label pattern already verified by hand for the five-destination IA rework is reused here, not reinvented. | TBD | TBD | TBD |
| Today's priorities — supporting workflows deliberately out of scope this stage | Per the user's own phasing ("start with Today's priorities, urgent alerts and completion list, then extend the supporting workflows"), this stage does not include: Intelligence's Investigate/Monitor/Defer/Dismiss actions with reasons and review dates (belongs to the Intelligence stage), a "significant changes since last visit" digest, a focused single-investigation working view with saved progress, or a defined research stopping condition. These remain in the dependency queue as their own stages, not silently dropped. | None yet - these are unimplemented, not broken. Today's priorities/urgent-alerts/completed-list stand on their own without them. | The scope boundary is stated explicitly in this stage's commit message and report, not implied by omission. | TBD | TBD | TBD |
| Today's "Completed today" — report entries use `uploadedAt` as a delivery proxy | `managed_reports` has no dedicated "delivered" timestamp; a published report's `uploadedAt` (if it falls today) is shown as "Report published" in the Completed today list. This is the same proxy-timestamp pattern already disclosed for growth-events' `investigation_saved`. | A report uploaded as a draft and only published later in the day would show the upload time, not the actual publish moment, if the two differ. | The list is filtered on `status === 'published'`, so a still-draft upload never appears; only the exact moment within "today" is approximate. | TBD | TBD | TBD |

| Intelligence investigation briefs — fact/interpretation separation is model-judged | `generateInvestigationBrief` asks the model to sort its own output into observedFacts (must cite evidence), interpretation and suggestedActions; a hallucination guard drops any fact citing an evidence key that wasn't actually supplied, and coverage gaps (no news, no social coverage, stale evidence, zero evidence) are computed deterministically in code, never by the model. The judgment of _which category a statement belongs in_ is still the model's own classification, not a rules engine. | A model could occasionally misclassify an inference as an observed fact (or vice versa) even though every fact it labels that way does carry a real citation. | Every fact's citation opens the actual source in an evidence panel, so a reviewer can verify the claim against the real text in one click rather than trusting the label alone; no confidence score is ever shown, so nothing implies false certainty. | TBD | TBD | TBD |
| Intelligence investigation briefs — no live verification of the evidence panel or save-to-project flow | Static review only: the Sheet component is Radix-based (keyboard/focus-trap handled by the library), fact citations are real `<button>` elements, and saveInvestigation was typechecked end-to-end against the new schema. No actual browser session exercised generating a brief against live mentions/news data, opening the evidence panel, or saving an investigation with a real project attached - no session credentials in this environment, the same constraint noted throughout this register. | An interaction correct in source could still behave unexpectedly against real AI output shapes or real evidence volumes. | Automated `tsc`/eslint/vitest/build all pass; the hallucination guard and deterministic-gap logic have direct unit coverage (investigation-brief-guards.test.ts). | TBD | TBD | TBD |

| Studio named versions — comparison and mobile-panel restore not built this stage | Per the same phasing discipline as Today's stage, this pass delivers named versions, list, restore-into-editor and campaign handoff (`draft_versions`, append-only by construction - no UPDATE/DELETE policy exists at the database layer, not just by convention). Side-by-side version comparison and a dedicated mobile drawer for the version list (reusing `EvidenceDrawer`'s pattern, per the research) were not built - the current list renders inline in the same stacked layout both cards and the version list already use responsively. | None yet - versions are usable and restorable today; comparison is a missing convenience, not a broken guarantee. | The append-only database policy means no future comparison/restore feature can accidentally lose a version, whatever UI is built on top later. | TBD | TBD | TBD |
| Studio named versions — "restore" only reaches the box it was saved from | A saved version is scoped to one suggestion index within one thread; restoring loads its content back into that same recommendation's textarea. There is no cross-suggestion or cross-thread restore, and versions saved from the raw tested-message box (not yet wired - only RecommendationCard has the save action this stage) don't exist yet. | A user cannot yet save a version of the original tested message itself, only of a generated recommendation. | The `source_message_id`/`suggestion_index` columns already generalise to that case without a schema change - only a UI hook-up remains. | TBD | TBD | TBD |
| Studio named versions — no live verification | Static review only: `tsc`/eslint/vitest/build all pass, and RLS/append-only behaviour has direct unit coverage. No real browser session saved a version, restored one, or sent one to campaign prep against live data - no session credentials in this environment, the same constraint as every prior UI stage. | An interaction correct in source could still behave unexpectedly against real data volumes or concurrent edits. | Automated checks all pass; the workspace-membership guard on `saveDraftVersion` mirrors the exact pattern already verified for `linkItemToProject`/`createKnowledgeEntry`. | TBD | TBD | TBD |

| Campaigns 4-stage refinement — scoped to inline field validation on Post only this pass | The full request (explicit Objective&content/Accounts&audience/Timing/Review stepper, renewed-approval-on-material-change, client review portal) was not attempted whole. Research found: (a) `post-campaign.tsx`/`reply-campaign.tsx`/`engage-campaign.tsx` are already large, live, working single-page forms with an implicit anchor-based stepper and correct draft-preservation (everything is one page's state, so nothing is lost moving between sections) - rewriting 900-1300 line production components into an explicit multi-step wizard is a large, risky change with no confirmed customer complaint driving it; (b) "renewed approval on material change" has zero existing backend support (`publish_jobs` has no approval/approved_content_hash column, and it's unverified whether campaigns can even be edited post-creation) - building that from scratch this pass would have been speculative. What WAS delivered, safely: inline field-level validation errors on `post-campaign.tsx` (campaign name, message, persona selection), reusing the exact same validation checks `blockStep` already ran - purely additive, no schema or server change, zero risk to the live publish path. `reply-campaign.tsx` was not touched - its validation architecture is materially different (no `blockStep`/scroll pattern, only 2 `setError` call sites) and needs its own pass, not a copy-paste. | The explicit 4-stage stepper, approval-renewal and client review portal remain unbuilt. | Inline errors ship real, tested value today; the deferred pieces are recorded as their own follow-up rather than attempted unsafely under this stage's time budget. | TBD | TBD | TBD |

| Results/Performance — inferred-metric labelling and source-coverage fixed, not the full requested refinement | Scoped to the highest-integrity gap found: `reachOf()`'s amplification estimate and `engagementRate`'s zero-when-no-impressions default were previously rendered identically to real platform data, with no "estimated"/"not available" distinction anywhere in the UI - this pass added `reachIncludesEstimates`/`engagementRateAvailable` flags (computed in `performance.ts`/`performance.functions.ts`, covered by real unit tests in `performance.test.ts`) and labelled every headline stat that uses them, plus relabelled "Earned media value" to "Estimated earned media value" (it was always a CPM-based estimate, just unlabelled). Also fixed a real display/export mismatch: CSV export previously ignored the on-screen search/activity/tone filters entirely. NOT done this pass: connecting results to project/campaign/approved-content-version (performance data has no `project_id`/`draft_version_id` linkage at all - would need new joins through `publish_jobs`, itself a larger schema change); the narrative "highlights" text in `performance-report.ts` still says e.g. "resulting in an engagement rate of 0%" without the same availability caveat the headline stat cards now carry; and `reports.index.tsx`'s separate export path wasn't audited for the same filter-parity issue. | Report narrative text can still imply confirmed zero performance in prose even though the stat cards next to it now say "not available" for the same number - an inconsistency between two representations of the same fact within one page. `reports.index.tsx`'s export parity is unverified. | The three concrete, verifiable wins (estimate labelling, availability flags, export filter parity) ship now with real test coverage; the narrative-text and reports-index gaps are named explicitly rather than assumed fixed by association. | TBD | TBD | TBD |

| §9.7 Token caps and cost controls — update: real enforcement added for one of eight AI call sites | Supersedes the §9.7 row above for what's now true: a real, workspace-settable monetary spend limit exists (`workspace_budgets`, estimated from `ai_events.estimated_cost_usd` via `src/lib/ai-pricing.ts`'s own published-rate table - never a figure the provider confirms) and is enforced server-side on `sendMessage` (Response Studio message tests, the one AI call site already instrumented with ai-observability and the highest-cost feature in this app). The other seven direct-fetch AI call sites named in the §9.8 row remain completely unenforced and uninstrumented - this closes the gap for one path, not all of them. `plan_limits`'s tier-wide call-count caps remain unchanged and still soft-only. | Heavy usage through any of the seven unenforced call sites could still generate uncontrolled AI spend with nothing to stop it. | Enforcement runs inside the idempotent operation (see the idempotency row below), so a deduped retry never re-checks or re-blocks for work that already happened; absence of a spend limit means "no limit set," never a silent zero-budget block. | TBD | TBD | TBD |
| Idempotency scaffold wired into one flow (`sendMessage`) | `src/lib/platform/idempotency.server.ts` existed fully built but genuinely unused since Master Rules §8.1 adoption - no `idempotency_keys` table existed and nothing imported it. This pass added the migration and wired it into `sendMessage` (both `new.tsx` and `chat.$threadId.tsx` now generate and rotate a client-side key, matching `post-campaign.tsx`'s pre-existing convention for the same problem). No other server function uses it yet - the scheduler's own retry safety still comes from its `status='pending'→'running'` compare-and-swap, not this scaffold, and that hasn't changed. | A double-click or network retry on any OTHER chargeable action (campaign launch aside, which already has its own idempotency key) could still create a duplicate charge or duplicate row. | The one flow now wired has direct test coverage (`budget-guards.test.ts`) asserting the key rotates after success and that concurrent/duplicate submissions are rejected by the underlying `withIdempotencyKey` logic, which was already unit-tested when the scaffold was first built. | TBD | TBD | TBD |
| AI budget settings have no role gating | `setWorkspaceBudget` is workspace-scoped only (any member can set the spend limit) - matches every other workspace-settings server function in this codebase (`updateProjectContext`, `setKnowledgeApprovalStatus`, etc), none of which are role-gated server-side. Server-side admin-only enforcement doesn't exist anywhere in this app yet (see the §6.4 MFA row's own note on this same gap across ~90 admin call sites) - introducing one just for this function would have been an unverified, inconsistent one-off. | Any workspace member, not just an "authorised" one as the spec asked for, can raise or remove the spend limit. | The UI card states this limitation directly to the user rather than implying a permission boundary that doesn't exist. | TBD | TBD | TBD |
| No pre-flight cost summary in the Studio composer | The spec asked for "a concise cost summary before a chargeable task." The Lovable AI gateway gives no pricing API and no way to know a call's token count before it completes, so a genuine pre-flight dollar estimate isn't possible without fabricating one - which this pass's whole point was to stop doing elsewhere. The AI budget panel (Profile → Plan & usage) and the spend-limit error itself (thrown at the moment a request would exceed the limit) are the closest honest equivalents; no persistent "estimated cost" caption was added to the shared Composer component, which is a delicately-styled, hero-variant component shared across entry points. | A user has no cost signal at the exact moment of clicking "Test message," only after the fact or once blocked. | None - explicitly deferred rather than faked. | TBD | TBD | TBD |

## How to use this register

1. When a Master Rule can't be met before a release, add a row here instead of silently shipping the gap.
2. Fill in **Owner** and **Deadline** before merging — "TBD" is a placeholder for the adoption pass, not a resting state.
3. When an item is resolved, delete the row (git history keeps the record) rather than marking it "done" in place.
4. This file should shrink over time. A growing register is a signal to stop adding scope and pay down risk instead.
